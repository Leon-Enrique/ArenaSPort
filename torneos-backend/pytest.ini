[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
filterwarnings =
    # Starlette avisa de un import que hace FastAPI internamente, no es
    # nuestro y no hay nada que arreglar de este lado.
    ignore::DeprecationWarning:starlette.*
